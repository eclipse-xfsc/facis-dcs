image:
  repository: postgres
  tag: "16-alpine"
  pullPolicy: IfNotPresent

service:
  port: 5432

auth:
  username: dcs
  password: dcs
  database: dcs

persistence:
  enabled: false
  size: 8Gi
  accessModes:
    - ReadWriteOnce

resources: {}
